export enum StatusCodes {
  // SUCCESS
  OK = 200,
  CREATED = 201,
  NO_CONTENT = 204,
  // ERROR
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  CONFLICT = 407,
  INTERNAL_SERVER_ERROR = 500,
}
